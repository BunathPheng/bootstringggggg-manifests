---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bootstringggggg                  # Ingress name
  labels:
    app: bootstringggggg
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "bootstringggggg.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bootstringggggg               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
